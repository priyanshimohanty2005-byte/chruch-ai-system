{
  "name": "propresenter-local-bridge",
  "version": "1.0.0",
  "description": "Local MCP server for ProPresenter. Must run ON or NEAR the ProPresenter computer's network, since ProPresenter's API is local-network only.",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.13.0",
    "zod": "^3.24.1"
  }
}
